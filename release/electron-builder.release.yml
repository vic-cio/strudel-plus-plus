# Release build configuration (electron-builder 25.1.8).
# Generated by scripts/generate-release-config.mjs from environment.
# This overrides package.json only for the release workflow.
mac:
  identity: 'DEVELOPER_ID_APP_IDENTITY_PLACEHOLDER'
  notarize: true
  hardenedRuntime: true
  target:
    - dmg
    - zip
